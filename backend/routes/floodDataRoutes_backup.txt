const express = require('express')
const router = express.Router()
const {
  getDistrictData,
  getMapID,
  getDatafromMongo,
  getPolygons,
  rando,
} = require('../controllers/earthEngineController')
const { protect } = require('../middleware/authMiddleware')

router.post('/flood-data', getDistrictData)
router.post('/polygons', getPolygons)
router.post('/district', getMapID)
router.get('/test-boi', getDatafromMongo)
router.get('/testo', protect, rando)

module.exports = router
