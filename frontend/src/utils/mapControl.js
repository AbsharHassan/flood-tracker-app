import { useEffect } from 'react'
import { createRoot } from 'react-dom/client'
import { render } from 'react-dom'

export const useMapControl = (map, controlPosition, component) => {
  useEffect(() => {
    console.log('function has been called')
  })
}
